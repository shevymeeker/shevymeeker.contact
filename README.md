# shevymeeker.contact

This repo holds the landing/portfolio hub. See `portfolio-pwa-blueprint.md` for the React + MUI + Dexie offline-first architecture that turns this into a multi-app PWA portfolio with per-app pages, downloads, and an essay feed.
